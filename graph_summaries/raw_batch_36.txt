function::compare_weights.py::recover_factor | This function calculates a scaling factor by performing a low-rank SVD on the delta tensor and normalizing the largest singular value. It returns zero if the denominator norm is too small to avoid division by zero.
function::compare_weights.py::count_layers | This function counts the number of layers by extracting layer indices from specific MLP down projection weight keys in the shard map. It returns the maximum index plus one, or zero if no valid indices are found.
function::config.py::_resolve_ctx_size | This function retrieves and validates the context size from an environment variable, ensuring it exceeds a safety margin of 5000 tokens. It returns the parsed value minus the safety margin or raises an error if the input is invalid or too small.
function::config.py::_load_or_generate_keypair | This function ensures a keys directory exists and loads an existing RSA private key or generates a new 2048-bit key pair. It saves the private key to a file with restricted permissions and returns both the private key object and the public key in PEM format.
function::config.py::qwen_llm_cfg | This function constructs a configuration dictionary for a qwen-agent LLM client pointing to a local llama.cpp server. It sets the model name, server URL, and maximum input tokens based on provided arguments or default values.