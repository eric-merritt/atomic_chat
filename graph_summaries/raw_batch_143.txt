function::tests/test_accounting_fifo_lifo.py::test_inventory_valuation_fifo | This test verifies the FIFO inventory valuation logic by checking the total cost and quantity of items. It asserts that the calculated values match the expected result of 4500.00 for 20 units.
function::tests/test_accounting_inventory.py::_load | This helper function checks if the input is already a dictionary and returns it directly. Otherwise, it parses the input string as JSON and returns the resulting dictionary.
function::tests/test_accounting_inventory.py::test_register_item | This test registers a new inventory item with specific details like SKU and price. It asserts that the operation returns a success status and the correct SKU.
function::tests/test_accounting_inventory.py::test_register_duplicate_sku | This test attempts to register an item with a SKU that already exists in the database. It asserts that the operation returns an error status with a message indicating the item already exists.
function::tests/test_accounting_inventory.py::test_receive_inventory | This test registers an item and then receives inventory for it, creating a journal entry. It asserts that the inventory and cash account balances are updated correctly to reflect the transaction.