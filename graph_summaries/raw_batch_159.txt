function::tests/test_context.py::test_serialize_tool_result_truncates | This test verifies that the serialize_tool_result function truncates long content to a maximum length of 4000 characters. It generates a 5000-character string and asserts that the resulting content length is exactly 4000.
function::tests/test_conversation_tasks.py::test_conversation_task_fields | This test verifies that the ConversationTask model contains the expected database columns. It asserts that id, conversation_id, title, status, and created_at are present in the table schema.
function::tests/test_conversation_tasks.py::test_default_status | This test checks that the default value for the status column is set to 'pending'. It retrieves the column definition and asserts that its default argument matches the expected string.
function::tests/test_conversation_tasks.py::test_status_values | This test ensures that the status field accepts only the allowed values: pending, active, and done. It creates a task instance for each status and asserts that the value is stored correctly.
function::tests/test_integration.py::agent_available | This function checks if an MCP agent is responding on a specified port by attempting to initialize a client session. It returns True if the connection and initialization succeed, otherwise it returns False.