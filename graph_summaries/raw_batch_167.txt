function::tests/test_routes_files.py::test_read_traversal_rejected | This test verifies that directory traversal attempts in the file read endpoint are rejected with a 403 status code. It ensures that accessing system files like /etc/passwd via path traversal is blocked.
function::tests/test_routes_files.py::test_read_direct_system_path_rejected | This test checks that directly requesting a system path like /etc/passwd is rejected with a 403 status code. It confirms that the endpoint prevents access to sensitive system files.
function::tests/test_routes_files.py::test_serve_image | This test creates a temporary PNG image file and requests it via the serve endpoint. It asserts that the response status code is 200, confirming the image is served correctly.
function::tests/test_tool_autodiscovery.py::test_all_existing_tool_modules_still_register | This test ensures that the auto-discovery mechanism registers all expected tool modules. It compares the registered tools against a hardcoded set and fails if any are missing.
function::tests/test_tool_autodiscovery.py::test_underscore_modules_not_loaded_as_tool_modules | This test verifies that underscore-prefixed modules are not loaded as top-level tool modules. It checks that these internal modules are not directly targeted by the auto-discovery process.