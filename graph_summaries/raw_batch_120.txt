function::tools/web.py::_get_user_id | This function attempts to retrieve the ID of the currently authenticated user from Flask-Login. It returns None if the user is not authenticated or if a RuntimeError occurs.
function::tools/web.py::_store_summary | This function generates a unique reference ID and stores a summary dictionary in the database. It also cleans up old records based on a time-to-live interval before inserting the new data.
function::tools/web.py::_load_summary | This function retrieves a summary from the database using a specific reference ID and optional user ID. It returns the parsed JSON data if found, or None otherwise.
function::tools/web.py::_load_tube_site_selectors | This function loads and merges site-specific selectors from a shipped file and a user-local configuration file. It caches the result based on the shipped file's modification time to avoid unnecessary re-reading.
function::tools/web.py::_match_site_by_url | This function finds the site configuration that matches the host of the provided URL. It checks for exact matches or substring matches between the URL host and the site host.
function::tools/web.py::_detect_content_type | This function detects the content type of a page by checking for specific site selectors or generic HTML attributes. It returns types like 'video_gallery' or 'photo_gallery' based on the detected elements.
function::tools/web.py::_build_selector | This function generates a CSS selector string for a given BeautifulSoup element. It prioritizes class names, then IDs, and falls back to just the tag name.
function::tools/web.py::_score_card | This function analyzes a container element to find selectors for sub-elements like links, thumbnails, and titles. It returns a dictionary containing the found selectors.
function::tools/web.py::_first_matching_url | This function searches for the first valid URL within a container based on provided tag and attribute selectors. It validates the URL extension and returns the first matching value found.
function::tools/web.py::_page_summary_with_site_selectors | This function parses HTML content and extracts items using specific container and card selectors from a site configuration. It normalizes the card configuration to handle both single dictionaries and lists.