class::tools/vision.py::DescribeImageTool | This class defines a tool that converts an image to Base64 and sends it to a vision model for analysis. It returns a text description of the image based on the provided path and prompt.
function::tools/vision.py::call | This method parses input parameters to get the image path and prompt, then imports necessary libraries. It defines a helper function to encode the image and prepares to send it to the vision API.
function::tools/vision.py::encode_image | This function reads an image file from the specified path in binary mode. It encodes the file content into a Base64 string and returns it as a UTF-8 decoded string.
function::tools/vision.py::get_mime_type | This function extracts the file extension from the given file path and converts it to lowercase. It returns the corresponding MIME type for common image formats, defaulting to jpeg if unknown.
function::tools/web.py::_strip_html_noise | This function removes non-structural HTML elements such as head, script, style, and comments from the input string. It cleans the HTML to retain only the structural body content for LLM consumption.
function::tools/web.py::_apply_cookies | This function sets cookies on the shared session with proper domain scoping derived from the URL. It ensures the domain includes a leading dot so cookies match the root domain and all subdomains.
function::tools/web.py::_get_or_create_browser | This function retrieves an existing headless Firefox browser instance or creates a new one if it does not exist. It initializes the WebDriver with the specified geckodriver path or an environment variable.
function::tools/web.py::_handle_cf_challenge | This function waits for a Cloudflare challenge to appear and then clicks the checkbox to solve it. It returns True if a challenge was detected and handled successfully within the timeout period.
function::tools/web.py::_store_page | This function stores the stripped page content in a global store with a unique reference ID and timestamp. It also cleans up any expired entries from the store before saving the new content.
function::tools/web.py::_load_page | This function retrieves the stored page content associated with the given reference ID. It returns the content dictionary if found, or None if the reference does not exist.