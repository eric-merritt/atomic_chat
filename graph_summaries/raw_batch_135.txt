function::routes/files.py::upload_file | This function handles file uploads by validating the presence of the file and checking its extension against an allowed list. It saves the file with a unique UUID-based name to the upload directory and returns the saved path.
function::routes/files.py::serve_file | This function retrieves a file path from query parameters and validates it for security and existence. It returns the requested file if valid, or an appropriate error message if the path is missing, invalid, or not found.
function::routes/health.py::_check_db | This function checks database connectivity by executing a simple SELECT 1 query and measuring the execution latency. It returns a tuple indicating success, the latency in milliseconds, and any error message if the query fails.
function::routes/health.py::_check_llama | This function probes the llama-server health endpoint to check if the service is alive and measure the response latency. It returns a tuple with the health status, latency, and the loaded model ID if the server is healthy.
function::routes/health.py::health | This function serves as a basic liveness check endpoint to confirm that the process is alive and serving HTTP requests. It returns a JSON response with a status of "ok".